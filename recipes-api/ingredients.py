# class Ingredient:
#   def __init__(id, name, uuid):
#     self.id = id
#     self.name = name
#     self.uudi = uuid

# EXISTS_QUERY = "SELECT * FROM ingredients WHERE name=%s"
# def exists(name, cursor):
#   print(cursor.execute(EXISTS_QUERY, name))
#   return false